# Templates

Templates are reusable policy snippets, not evidence that the corresponding
output already works. Complete validation-tested manifests live in `examples/`.

Profiles describe intent. Providers resolve package-level instructions for an
exact upstream release, and evidence determines whether a resolved combination
is supported.
